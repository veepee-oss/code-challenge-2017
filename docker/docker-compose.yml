version: '2'
services:

    # See: docker/Dockerfile
    code_challenge_server:
        build: .

        restart: always

        container_name: docker_code_challenge_server

        ports:
            - "8080:80"

        volumes:
            - ../.:/var/www/html

        network_mode: "bridge"
